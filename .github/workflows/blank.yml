name: Hello Shivathmika Action

# Run this workflow every time you push code
on:
  push:
    branches:
      - main

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "🎉 Hello Shivathmika! Your GitHub Action ran successfully!"
